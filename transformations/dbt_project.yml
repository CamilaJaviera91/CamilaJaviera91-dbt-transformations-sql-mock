name: 'transformations'
version: '1.0.0'

# Este es el perfil de conexión que usará dbt
profile: 'transformations'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  transformations:
    example:
      +materialized: view
